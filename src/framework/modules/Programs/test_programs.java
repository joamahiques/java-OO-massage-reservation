package framework.modules.Programs;
import java.util.Date;

import framework.classes.Dates;
public class test_programs {

	public static void main(String[] args) {
		/*String masaje="";
		float precio=0;
		float duracion=0;
		String dstart="";
		Boolean ducha=false;
		String dateend="";*/
		//masaje=Functions_data.askmassage(menuMassages.opt);
		Dates date;
		Dates dateexit;
		String sauna="";
		Dates datesauna=new Dates();
		Dates dateENDsauna=new Dates();
		Date fecha;
		float price=0;
		String duration="30 min";
		Dates diference=new Dates();
		//int numdays=diference.subst2datesDates(date, dateexit);
		//ArrayList<Calendar> entreDates=new ArrayList<Calendar>();
		
		
		//FunctionsDummies.askDNIRelax();
		//FunctionsDummies.credumm(0);
		//String activity=Functions_data.OptionsActivity("Do you want a activity?", "ACTIVITY");
		//ArrayList<Dates> fechas=new ArrayList<Dates>();
//		date=FunctionsDates.datestart("Intro a date DD/MM/YYYY", "DATE");
//		dateexit=FunctionsDates.dateend("Intro a exit date","EXIT",date);
//		int numdays=diference.subst2datesDates(date, dateexit);
		//date=FunctionsDates.updatedatestart("Intro a date", "DATE",dateexit);
		/*sauna=Functions_data.OptionsActivity("Do you eant an activity?", "ACTIVITY");
		datesauna=FunctionsDates.datesActivity("Date of activity",sauna,date, dateexit);
		dateENDsauna=FunctionsDates.datesActivity("End date of activity",sauna,datesauna, dateexit);*/
		//entreDates=datesauna.getBetweenDates(date, dateexit);
		//JOptionPane.showMessageDialog(null, entreDates);
		//price=Functions_data.price(duration, numdays, activity);
//		System.out.println(price+" price");
		
	}

}
